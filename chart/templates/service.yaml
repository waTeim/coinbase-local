apiVersion: v1
kind: Service
metadata:
  name: {{ include "coinbase-local.fullname" . }}
  labels:
    {{- include "coinbase-local.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coinbase-local.selectorLabels" . | nindent 4 }}

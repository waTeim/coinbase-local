apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coinbase-local.fullname" . }}-test-connection"
  labels:
    {{- include "coinbase-local.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coinbase-local.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
